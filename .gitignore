# Python
__pycache__/
*.py[cod]
*.so
.env
.venv/
venv/
build/
dist/
*.egg-info/
.DS_Store

# Project specific
dubbing_work/
temp/
*.wav
*.mp4
*.srt
*.zip

# OAuth credentials (sensitive files)
youtube_credentials.json
youtube_credentials_new.json
youtube_token.pickle
cookies.txt
cookies.json

# Fonts (keep local fonts in repo for server deployment)
# fonts/ is intentionally not ignored to ensure server compatibility
