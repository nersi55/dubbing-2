sudo apt update

sudo apt upgrade -y

sudo apt upgrade -ysudo apt install -y python3 python3-pip python3-venv git ffmpeg
or sudo apt install -y python3 python3-pip python3-venv git ffmpeg

cd ~

git clone https://github.com/nersi55/dubbing-2.git

cd dubbing-2

python3 -m venv venv

source venv/bin/activate

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

//for font 
sudo apt update
sudo apt install fonts-vazirmatn
sudo apt install fonts-vazirmatn


pip install -r requirements.txt


receive the lateset version form github
git fetch origin
git reset --hard origin/main
git clean -fd